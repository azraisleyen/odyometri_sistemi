# GUI User Guide

Run `gradle run` or `gradle runSimulation`. The dashboard shows connection mode, serial controls, test controls, current state, real-time audiogram, event log, and results table.

For simulation, press Start, Present tone, Mark RESPONSE, or Auto simulate step. For Proteus, select a COM port, baud rate 9600, connect, and ensure Arduino sends `RESPONSE` on button press.
